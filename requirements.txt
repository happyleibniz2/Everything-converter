# ==========================
# Everything Converter
# Python 3.12+
# install dependencies with: pip install -r requirements.txt
# ==========================

# --- Required ---

# GUI toolkit
PySide6>=6.11.1

# Fluent Design widget set (navigation, cards, theming)
PySide6-Fluent-Widgets>=1.11.3

# Image conversion, thumbnails
Pillow>=11.3.0

# Process control for pause / resume / cancel of ffmpeg
psutil>=7.0.0

# HEIF/HEIC read and write support for Pillow
pillow-heif>=0.18.0

# --- PDF / Office / Archive conversion (converters/document_converter.py,
#     pdf_converter.py, archive_converter.py) ---

# PDF text extraction & page rendering
PyMuPDF>=1.26.0

# PDF merge / split / metadata
pypdf>=5.0.0

# Word documents (.docx -> PDF/text)
python-docx>=1.2.0

# Excel workbooks (.xlsx -> CSV/PDF)
openpyxl>=3.1.5

# PowerPoint decks (.pptx -> PDF/images)
python-pptx>=1.0.2

# 7-Zip archives
py7zr>=1.0.0

# RAR archives (needs the system `unrar`/`unar` binary at runtime)
rarfile>=4.2

# --- 3D model conversion (converters/model_converter.py, usd_export.py) ---

# Mesh loading/exporting for STL/OBJ/glTF/GLB/PLY/OFF/3MF/DAE/FBX
trimesh>=4.5.0

# Geometry math used by trimesh and the renderer
numpy>=2.0.0

# USD / USDA / USDC / USDZ export via the official OpenUSD Python bindings
usd-core>=24.11

# --- Bundled, not installed via pip ---
# FFmpeg and ffprobe ship in ./ffmpeg and handle all audio/video work.
